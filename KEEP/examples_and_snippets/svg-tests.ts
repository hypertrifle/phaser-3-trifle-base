
    //     let scalar = 5;


    //     for(i in svg){
    //         let entry = svg[i];
    //         const regex = /viewBox="(\d+) (\d+) (\d+) (\d+)"/gm;
    //         let matches = regex.exec(entry);

    //         entry = entry.replace("<svg", "<svg width=\""+( (parseInt(matches[3]) - parseInt(matches[1])) * scalar )+ " \" height=\""+ ( (parseInt(matches[4]) - parseInt(matches[2])) * scalar ) +"\"" );
            
    // }
