# Look for the different tagged releases at https://hub.docker.com/r/library/node/tags/
image: node:8

before_script:
- npm install
lint:
  script:
  - npm run tslint
build:
  script:
  - npm run build