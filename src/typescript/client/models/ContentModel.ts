/**
 * add all tranlatable / configurable content to this class
 *
 * @export
 * @class ContentModel
 */
export default class ContentModel {
    strings: { [key: string]: string } = {
        test: "TestString"
    }
}

