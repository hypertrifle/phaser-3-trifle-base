/**
 * This is where we can define usual display based helpers, there are a ton in Phaser alredy, but these can be useful for other sttuff.
 *
 * @class DisplayUtils
 */
export default class DisplayUtils {
   constructor(){
   }
   resizeSVGAsset( asset: Phaser.Loader.FileTypes.SVGFile){
    console.log(asset);
  }
}
