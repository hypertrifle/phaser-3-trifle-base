/**
 * This is where we can define usual math helpers, there are a ton in Phaser alredy, but these can be useful for other sttuff.
 *
 * @class MathUtils
 */
export default class MathUtils {
   static poop(e?: number): string {
     return 'poop';
  }
}
